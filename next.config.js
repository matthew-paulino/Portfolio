/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['assets.unsplash.com'],
  },
}

module.exports = {
  images: {
    domains: ['assets.unsplash.com'],
  },
}


module.exports = nextConfig
